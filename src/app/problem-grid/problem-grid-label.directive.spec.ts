import { Component } from '@angular/core';
import { ProblemGridLabelDirective } from './problem-grid-label.directive';

describe('ProblemGridLabelDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ProblemGridLabelDirective(undefined, undefined, undefined);
  //   expect(directive).toBeTruthy();
  // });
});
