import { ProblemGridRectDirective } from './problem-grid-rect.directive';

describe('ProblemGridRectDirective', () => {
  it('should create an instance', () => {
    const directive = new ProblemGridRectDirective();
    expect(directive).toBeTruthy();
  });
});
